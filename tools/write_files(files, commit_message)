import git

def write_files(files, commit_message):
    repo = git.Repo('.')
    output = []
    for f in files
    path =f['path']
    contents = f['contents']
    if os.path.isabs(path) or '..' in path.split(os.sep):
        return f"Error: invalid path {path}"
        with open(path, 'w', encoding='utf-8') as fh:
            fh.write(contents)
            repo.index.add([path])
            if path.endswith('.py'):
                output.append(doctests(path))
                repo.index.commit(f"[docchat] {commit_message}")
                return '/n'.join(output) if output else "Files written and committed."