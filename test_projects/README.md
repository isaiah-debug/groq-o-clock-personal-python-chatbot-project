# Test Projects

This folder is reserved for the required git submodules from previous projects:

- webpage
- markdown_compiler
- ebay_scraper

Add them with `git submodule add <url> test_projects/<name>` after you know the correct repository URLs.
